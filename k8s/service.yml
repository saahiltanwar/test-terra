apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  type: LoadBalancer       # exposes a public IP; use ClusterIP if internal only
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80             # external port
      targetPort: 8181     # TODO: must match containerPort in deployment.yml
